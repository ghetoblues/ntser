node-linker=hoisted
# Vite 8 ships Rolldown as optional per-platform native bindings. pnpm 8's
# hoisted linker skipped them in CI, so `vite build` could not start.
package-import-method=clone-or-copy
